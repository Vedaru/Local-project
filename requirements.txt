openai>=1.0.0
python-dotenv>=1.0.0
chromadb>=0.4.0
jieba>=0.42.1
pyaudio>=0.2.13
requests>=2.31.0
pyyaml>=6.0

# Avatar 模块依赖（PyQt6 + WebEngine）
PyQt6>=6.4.0
PyQt6-WebEngine>=6.4.0

# 语音识别与音频处理（Project Seeka — 听觉模块）
# 注意：支持两种 Whisper 实现
# 选项 1（推荐，支持 CUDA）：faster-whisper + ctranslate2
#   pip install faster-whisper ctranslate2
# 选项 2（备选，稳定）：openai-whisper + PyTorch
#   pip install openai-whisper
# 目前已包含备选方案，模块会自动选择可用的库
openai-whisper>=20250101
faster-whisper>=0.5.0
ctranslate2>=4.0
numpy>=1.22.0,<2.3.0

# 电脑控制模块依赖
pyautogui>=0.9.53
pygetwindow>=0.0.9

# Agent / 浏览与检索能力（用于 modules.agent）
# 使用国内友好的爬虫流：requests + BeautifulSoup，优先访问 baidu（不回退到 Bing）
beautifulsoup4>=4.12.0
requests>=2.31.0
# 可选：DrissionPage（用于需要 JS 渲染的页面，非必须）
drissionpage>=1.1.0

# OCR（Tesseract 后端）
# - Python 端库：pytesseract
# - 还需在系统上安装 Tesseract 可执行文件（Windows 可通过 winget/choco 或官网安装），
#   并可通过环境变量 TESSERACT_CMD 指定其路径；默认识别语言可通过 TESSERACT_LANG 设置（例如：chi_sim+eng）。
pytesseract>=0.3.10

