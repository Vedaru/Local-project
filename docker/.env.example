# Docker Compose 环境变量模板
# 用法: copy .env.example .env 并填写 GATEWAY_API_KEY

GATEWAY_API_KEY=your-secure-api-key-here

# 可选：LLM 与模型（容器内 agent/orchestrator 使用）
ARK_API_KEY=
MODEL_NAME=deepseek-v4-pro

# 可选：运行环境（development | production）
APP_ENV=production
